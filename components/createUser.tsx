import { useState } from "react"
import { createUser } from "../fetchers/createUser"

export function CreateUser()
{

    const [billing, changeDate] = useState(new Date())
    const [mailing, changeGuestCount] = useState(1)
    const [payment, setName] = useState("")
    const [name, setPhone] = useState("")

    return <>
        
    
    </>
}